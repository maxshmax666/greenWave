import 'package:flutter/material.dart';

/// Centralized color definitions for the app.
/// The left panel uses a green background with black text.
/// This combination yields a contrast ratio of ~7.55:1 (WCAG AA compliant).
class AppColors {
  static const leftPanelBg = Color(0xFF4CAF50); // Colors.green[500]
  static const leftPanelText = Colors.black;
}
